plugins {
    id 'org.jetbrains.intellij' version '0.4.19'
}

group 'org.jige'
version '1.0'

// See https://github.com/JetBrains/gradle-intellij-plugin/
intellij {
    version '2020.1'
    localPath 'D:\\sft\\ideaIC-2020.1.win'
    plugins = ['java']
}
patchPluginXml {
    changeNotes """
      1.0 支持搜索java controller 里面的url 搜索可能不精确 实现思路是搜索注解名字包含controller/mapping的类和函数 然后获取里面的value值 
      """
}
test {
    systemProperty "idea.home.path", "D:\\sft\\ideaIC-2020.1.win"
}
//提交插件配置
publishPlugin {
    token = System.getenv("ORG_GRADLE_PROJECT_intellijPublishToken")
}